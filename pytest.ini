[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    e2e: tests de punta a punta que necesitan el servicio levantado
filterwarnings =
    ignore::DeprecationWarning
